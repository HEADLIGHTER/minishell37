#include "minisell.h"

int	main(int argc, char **argv, char **envp)
{
	t_env *tmp;
	t_shell *sh;
	int i;

	i = 1;
	sh = get_shell(INIT);
	sh->env = parser_env(envp);
	sh->line = NULL;

	while (i == 1)
	{
		sh->cmd = NULL;
		sh->cmd = parsing(sh->line, sh->env, &sh->last_exit);
	}
//	char *str = strdup("ex\"port");
